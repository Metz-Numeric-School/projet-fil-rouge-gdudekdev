-rewrite special function to handle each route, make a folder to store them all if needed

-change the way the routes are handled and generate more precise routes, the methods are very specific

-change all the displays considering those changes and create relevant patterns to send data, not only in headers


